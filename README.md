# SmartSite-Assistant
SmartSite Assistant is a RAG-based AI tool that converts any website into an interactive knowledge base. Given a URL, it crawls and extracts structured and unstructured content, generates embeddings, and enables users to ask natural language questions with fast, context-grounded answers.
